import os
import json
import certifi
import requests
import re
from dotenv import load_dotenv
from langchain_mcp_adapters.client import MultiServerMCPClient
from langchain.chat_models import init_chat_model
from langchain.agents import create_agent

load_dotenv()
os.environ["SSL_CERT_FILE"] = certifi.where()


# -----------------------------
# AUTHENTICATION
# -----------------------------
def get_azure_access_token():
    tenant = os.getenv("AZURE_TENANT_ID")
    client_id = os.getenv("AZURE_CLIENT_ID")
    client_secret = os.getenv("AZURE_CLIENT_SECRET")

    url = f"https://login.microsoftonline.com/{tenant}/oauth2/v2.0/token"

    payload = {
        "client_id": client_id,
        "client_secret": client_secret,
        "scope": "https://analysis.windows.net/powerbi/api/.default",
        "grant_type": "client_credentials",
    }

    response = requests.post(url, data=payload)
    response.raise_for_status()
    return response.json()["access_token"]


# -----------------------------
# EXECUTE DAX (Power BI REST)
# -----------------------------
def execute_dax_query(dax_query: str):
    dataset_id = os.getenv("POWERBI_DATASET_ID")
    token = get_azure_access_token()

    url = f"https://api.powerbi.com/v1.0/myorg/datasets/{dataset_id}/executeQueries"

    headers = {
        "Authorization": f"Bearer {token}",
        "Content-Type": "application/json",
    }

    body = {
        "queries": [{"query": dax_query}],
        "serializerSettings": {"includeNulls": True},
    }

    response = requests.post(url, headers=headers, json=body, timeout=60)
    response.raise_for_status()

    return response.json()


# -----------------------------
# EXTRACT MCP SCHEMA
# -----------------------------
def extract_mcp_schema(result):
    for msg in result["messages"]:
        if msg.type == "tool":
            artifact = msg.artifact
            if artifact and "structured_content" in artifact:
                return artifact["structured_content"]
    return None


# -----------------------------
# EXTRACT DAX FROM LLM RESPONSE
# -----------------------------
def extract_dax_from_response(llm_text: str):
    match = re.search(r"```DAX\s*(.*?)```", llm_text, re.DOTALL | re.IGNORECASE)
    if match:
        return match.group(1).strip()
    return None


# -----------------------------
# EXTRACT NUMERIC RESULT CLEANLY
# -----------------------------
def extract_numeric_result(data):
    try:
        rows = data["results"][0]["tables"][0]["rows"][0]
        key = list(rows.keys())[0]
        value = rows[key]
        return key, value
    except Exception:
        return None, None


# -----------------------------
# CONTEXT HOLDER
# -----------------------------
class FinOpsContext:
    def __init__(self, model, schema):
        self.model = model
        self.schema = schema


# -----------------------------
# INITIAL SETUP (RUNS ONCE)
# -----------------------------
async def finops_setup():

    print("🔐 Getting Azure Token...")
    token = get_azure_access_token()

    servers = {
        "powerbi-remote": {
            "transport": "http",
            "url": "https://api.fabric.microsoft.com/v1/mcp/powerbi",
            "headers": {"Authorization": f"Bearer {token}"}
        }
    }

    client = MultiServerMCPClient(servers)
    tools = await client.get_tools()

    model = init_chat_model(
        os.getenv("MODEL"),
        model_provider="openai",
        api_key=os.getenv("GITHUB_TOKEN"),
        base_url=os.getenv("ENDPOINT"),
    )

    system_prompt = """
You are a FinOps assistant.

Rules:
- Use schema metadata to understand dataset
- Generate valid DAX queries when calculations are requested
- Never hallucinate tables or columns

TIME RULES:
- Never use TODAY()
- Always determine latest available billing date using MAX(cost[date])
"""

    agent_graph = create_agent(model, tools, system_prompt=system_prompt)

    workspace_id = os.getenv("POWERBI_WORKSPACE_ID")
    dataset_id = os.getenv("POWERBI_DATASET_ID")

    print("📊 Loading schema from MCP...")

    schema_query = f"""
Inspect the semantic model.

Workspace ID: {workspace_id}
Dataset ID: {dataset_id}
"""

    result = await agent_graph.ainvoke(
        {"messages": [{"role": "user", "content": schema_query}]}
    )

    schema = extract_mcp_schema(result)

    print("✅ Schema cached successfully")

    return FinOpsContext(model, schema)


# -----------------------------
# CHAT FUNCTION (CLEAN OUTPUT)
# -----------------------------
async def chat_instance_stream(user_question, context: FinOpsContext):

    schema_json = json.dumps(context.schema)

    prompt = (
        "You are a FinOps expert.\n\n"
        "Use the schema to answer the question.\n"
        "If calculation is required, generate ONLY a DAX query inside ```DAX``` block.\n\n"
        "Schema:\n"
        + schema_json +
        "\n\nUser Question:\n"
        + user_question
    )

    response = await context.model.ainvoke(prompt)
    llm_text = response.content

    dax_query = extract_dax_from_response(llm_text)

    if not dax_query:
        return "⚠️ Unable to generate query for this request."

    try:
        raw_result = execute_dax_query(dax_query)
        key, value = extract_numeric_result(raw_result)

        if value is None:
            return "⚠️ Could not parse result."

        formatted_value = f"{value:,.2f}"

        return f"""
## 💰 Cloud Cost Summary

### {formatted_value} NOK
"""

    except Exception as e:
        return f"⚠️ Query execution failed: {str(e)}"
