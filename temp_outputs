import os
import asyncio
import certifi
import requests
from dotenv import load_dotenv
from langchain_mcp_adapters.client import MultiServerMCPClient

# -----------------------------
# ENV SETUP
# -----------------------------
load_dotenv()
os.environ["SSL_CERT_FILE"] = certifi.where()


# -----------------------------
# AUTHENTICATION
# -----------------------------
def get_azure_access_token():
    tenant = os.getenv("AZURE_TENANT_ID")
    client_id = os.getenv("AZURE_CLIENT_ID")
    client_secret = os.getenv("AZURE_CLIENT_SECRET")

    if not tenant or not client_id or not client_secret:
        raise ValueError("Azure credentials missing in .env")

    url = f"https://login.microsoftonline.com/{tenant}/oauth2/v2.0/token"

    payload = {
        "client_id": client_id,
        "client_secret": client_secret,
        "scope": "https://analysis.windows.net/powerbi/api/.default",
        "grant_type": "client_credentials",
    }

    print("🔐 Requesting Azure token...")
    response = requests.post(url, data=payload)
    response.raise_for_status()

    print("✅ Azure token generated successfully")
    return response.json()["access_token"]


# -----------------------------
# INTENT CLASSIFIER
# -----------------------------
def classify_question(q: str) -> str:
    q = q.lower()

    if any(word in q for word in ["schema", "table", "column", "structure"]):
        return "schema"

    return "analysis"


# -----------------------------
# SESSION-BASED SCHEMA FETCH
# -----------------------------
async def fetch_schema_via_session(client, dataset_id):
    """
    Opens an MCP semantic session and retrieves schema.
    Required for tenants where direct tool calls return [].
    """

    tools = await client.get_tools()

    for tool in tools:
        if tool.name == "GetSemanticModelSchema":
            result = await tool.ainvoke({"artifactId": dataset_id})

            # Fabric may return schema wrapped inside a list
            if isinstance(result, list) and len(result) > 0:
                item = result[0]

                if hasattr(item, "artifact"):
                    return item.artifact["structured_content"]

                if isinstance(item, dict):
                    if "artifact" in item:
                        return item["artifact"]["structured_content"]

                    if "structured_content" in item:
                        return item["structured_content"]

    raise RuntimeError("❌ Unable to retrieve schema — MCP session not established.")


# -----------------------------
# PRINT SCHEMA
# -----------------------------
def print_schema(schema):
    print("\n================ DATASET SCHEMA ================\n")

    for table in schema["Tables"]:
        print(f"📁 {table['Name']}")
        for col in table["Columns"]:
            print(f"   └─ {col['Name']} ({col['Type']})")
        print()

    print("\n🔗 Relationships:\n")
    for rel in schema["ActiveRelationships"]:
        print(f"{rel['PK']}  --->  {rel['FK']}")


# -----------------------------
# MAIN EXECUTION
# -----------------------------
async def main():
    print("🔐 Generating Azure access token...")
    token = get_azure_access_token()

    workspace_id = os.getenv("POWERBI_WORKSPACE_ID")
    dataset_id = os.getenv("POWERBI_DATASET_ID")

    if not workspace_id or not dataset_id:
        raise ValueError("Missing POWERBI_WORKSPACE_ID or POWERBI_DATASET_ID")

    # MCP Connection (workspace binding required)
    servers = {
        "powerbi-remote": {
            "transport": "http",
            "url": "https://api.fabric.microsoft.com/v1/mcp/powerbi",
            "headers": {
                "Authorization": f"Bearer {token}",
                "x-ms-powerbi-workspace-id": workspace_id,
                "Content-Type": "application/json"
            }
        }
    }

    print("🔌 Connecting to MCP server...")
    client = MultiServerMCPClient(servers)

    tools = await client.get_tools()
    print(f"✅ Connected. Available tools: {[t.name for t in tools]}")

    print("\n✅ FinOps MCP Client Ready.")
    print("Type your question (or 'exit' to quit).\n")

    while True:
        user_question = input("Ask FinOps question: ").strip()

        if user_question.lower() in ["exit", "quit"]:
            print("👋 Exiting.")
            break

        intent = classify_question(user_question)

        try:
            # -----------------------------
            # SCHEMA PATH
            # -----------------------------
            if intent == "schema":
                print("📡 Establishing session & fetching schema...")
                schema = await fetch_schema_via_session(client, dataset_id)
                print_schema(schema)

            # -----------------------------
            # ANALYSIS PATH (NEXT STEP)
            # -----------------------------
            else:
                print("⚠️ Analysis not implemented yet.")
                print("Next step: generate DAX locally and call ExecuteQuery.")

        except Exception as e:
            print("\n❌ MCP ERROR:")
            print(e)

        print("\n" + "-" * 60 + "\n")


# -----------------------------
# ENTRY POINT
# -----------------------------
if __name__ == "__main__":
    asyncio.run(main())
