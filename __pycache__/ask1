You are working on an existing React + FastAPI project.

The frontend is a React app that includes a real-time voice agent with an avatar (WebRTC + Azure Speech SDK). The backend is already complete and does NOT need any modification.

Your task is to integrate **Countly Web Analytics SDK** into the React frontend to track user activity.

---

GOAL:

Track:

1. How many users open the app (sessions)
2. Page views
3. Basic user interaction events (like starting and ending a session)

---

COUNTLY CONFIGURATION:

Use the following details:

COUNTLY_URL:
https://countly.green.we.nonprod.kubeit-int.dnv.com

APP_KEY:
cdeae8.....32a802

---

IMPORTANT CONSTRAINTS:

* Do NOT modify backend code
* Do NOT add unnecessary libraries
* Use ONLY "countly-sdk-web"
* Keep implementation simple and production-safe
* Avoid multiple initializations
* Use React best practices (useEffect, etc.)

---

FRONTEND REQUIREMENTS:

1. Install Countly SDK:

npm install countly-sdk-web

---

2. Initialize Countly ONLY ONCE when the app loads

* Do this in a top-level component (App.jsx or main.jsx)
* Use useEffect with empty dependency array

Example behavior:

* Initialize Countly
* Start session tracking
* Track initial page view

---

3. Track page views:

* Automatically track when the app loads
* Additionally, track specific pages like:

  * Dashboard
  * TalkCustomer (voice session page)

---

4. Track custom events:

Add event tracking for:

* When user starts a session
  event key: "session_started"

* When user ends a session
  event key: "session_ended"

These should be triggered from the existing TalkCustomer.jsx component:

* startSession() → trigger "session_started"
* endSession() → trigger "session_ended"

---

5. Ensure:

* Countly is not initialized multiple times
* No console errors
* Works even if user navigates between pages
* Does not break existing functionality

---

EXPECTED OUTPUT:

Provide:

1. Exact code for Countly initialization (App.jsx or main.jsx)
2. Code snippet for tracking page views
3. Modifications required in TalkCustomer.jsx for event tracking
4. Required imports
5. Clean and minimal implementation

Do NOT include long explanations.

Focus on correct, production-ready code that can be directly pasted.

---

OPTIONAL (if easy):

Track which page user is on using:
Countly.track_pageview("PageName")

---
